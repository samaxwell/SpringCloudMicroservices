spring:
  application:
    name: api-gateway

  sleuth:
    enabled: true
  zipkin:
    enabled: false

server:
  port: 8080

#spring:
#  application:
#    name: api-gateway-service
#  cloud:
#    gateway:
#      discovery:
#        locator:
#          enabled: true
#      routes:
#        - id: blog-service
#          predicates:
#            - Path=blogs/**
#            - Method=GET
#          uri: http://localhost:9090
#          filters:
#            - PrefixPath=/mypath

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always